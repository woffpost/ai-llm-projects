# Chatbot with Memory

Context-aware chatbot that remembers conversation history.

## How it works
Uses LangChain message history to pass full conversation context to the model on each request.

## Stack
- Python
- LangChain
- Anthropic API